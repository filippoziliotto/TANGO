I will provide examples of Pseudo-code program in response to given questions. For each new question, generate ONLY the related Pseudo-code program, given the examples you saw. Use only the functions that you have previously encountered.

Question: what color is the couch?
Program:
# search for the couch
explore_scene()
couch = detect("couch")

if is_found(couch):
    # answer the question
    navigate_to(couch)
    ans = answer("what color is the couch?")
    return ans

Question: what color is the dress in the closet?
Program:
# search for the closeth
explore_scene()
closet = detect("closet")

if is_found(closet):
    # answer the question
    navigate_to(closet)
    ans = answer("what color is the dress in the closet?")
    return ans

Question: which room is the dining table located in?
Program:
# search for the dining table
explore_scene()
dining_table = detect("dining table")

if is_found(dining_table):
    # answer the question
    navigate_to(dining_table)
    ans = answer("which room is the dining table located in?")
    return ans

Question: what is the color of the sink in the kitchen?
Program:
# search for the kitchen
explore_scene()
kitchen = detect("kitchen")

if is_found(kitchen):
    # search for the sink
    explore_scene()
    sink = detect("sink")

    if is_found(sink):
        # answer the question
        navigate_to(sink)
        ans = answer("what color is the sink?")
        return ans

Question: what color is the window curtain in the bedroom?
Program:
# search for the bedroom
explore_scene()
bedroom = detect("bedroom")

if is_found(bedroom):
    # search for the window curtain
    explore_scene()
    window_curtain = detect("window curtain")

    if is_found(window_curtain):
        # answer the question
        navigate_to(window_curtain)
        ans = answer("what color is the window curtain?")
        return ans


Now, respond to the new question.
